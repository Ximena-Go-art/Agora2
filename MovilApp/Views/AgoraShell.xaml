<?xml version="1.0" encoding="utf-8" ?>
<Shell
    x:Class="MovilApp.Views.AgoraShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MovilApp"
    xmlns:login="clr-namespace:MovilApp.Views.Login"
    xmlns:commons="clr-namespace:MovilApp.Views"
    xmlns:contentviews="clr-namespace:MovilApp.ContentViews"
    xmlns:viewmodels="clr-namespace:MovilApp.ViewModels"
    xmlns:admin="clr-namespace:MovilApp.Views.Admin"
    xmlns:views="clr-namespace:MovilApp.Views.Capacitaciones"
    FlyoutBehavior="Disabled"
    Title ="ÀgoraApp">

    <Shell.BindingContext>
        <viewmodels:AgoraShellViewModel />
    </Shell.BindingContext>

    <Shell.FlyoutHeader>
        <contentviews:FlyoutHeader />
    </Shell.FlyoutHeader>

    <Shell.FlyoutFooter>
        <contentviews:FlyoutFooter />
    </Shell.FlyoutFooter>

    <ShellContent
    Title="Iniciar sesión"
    ContentTemplate="{DataTemplate login:LoginView}"
    Route="Login" 
    IsVisible="{Binding UserIsLogout}"
        FlyoutItemIsVisible="False"/>
    <ShellContent
Title="Crear Cuenta"
ContentTemplate="{DataTemplate login:SignInView}"
Route="Registrarse" 
    FlyoutItemIsVisible="False"/>

    <FlyoutItem Route="nuestra_app" 
        FlyoutDisplayOptions="AsMultipleItems">
        <ShellContent
            Title="Home"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />

        <Tab Title="Admin Capacitaciones"
         Route="capacitaciones"
         Icon="paw.png">
            <ShellContent
                Title="Lista"
                Icon="cat.png"
                ContentTemplate="{DataTemplate admin:AdminCapacitacionesView}"
                Route="ListaCarreras" />
            <ShellContent
                Title="Agregar carrera"
                Icon="dog.png"
                ContentTemplate="{DataTemplate admin:AddEditCapacitacionesView}"
                Route="AgregarEditarCarrera" />
        </Tab>
    </FlyoutItem>
    <FlyoutItem Title="📖 Buscar Capacitaciones" Icon="search.png">
        <ShellContent
            Title="Buscar Capacitaciones"
            ContentTemplate="{DataTemplate views:CapacitacionesView}"
            Route="BuscarCapacitaciones" />
    </FlyoutItem>
    <MenuItem Text="Cerrar sesión"
      Command="{Binding LogoutCommand}"
      IconImageSource="random.png"/>

    

</Shell>